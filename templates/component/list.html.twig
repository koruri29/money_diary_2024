<div class="event-list-container">
	<h2 id="month">{{disp_year}}年{{disp_month}}月の支出</h2>
	<a href="top.php{{query_prev_month}}">&laquo;前月</a> | 
	<a href="#">今月</a> | 
	<a href="top.php{{query_next_month}}">来月&raquo;</a>
	<table>
		<thead>
			<tr class="item-row">
				<td></td>
				<td></td>
				<td>収支</td>
				<td class="plus-green">
					{% if  sum >= 0 %}+{{sum | number_format(0, '.', ',')}}{% endif %}
				</td>
				<td class="minus-red">
					{% if  sum < 0 %}{{sum | number_format(0, '.', ',')}}{% endif %}
				</td>
			</tr>
		</thead>
		<tbody>
			{% for key, item in items %}
				<tr class="item-row">
					<td>{{item.date | format_datetime('short', 'none')}}</td>
					<td class="list-other">{{item.other}}</td>
					<td>{{item.c_html | raw}}{{item.category_name}}</td>
					{% if item.option == 0 %}
						<td class="list-plus"></td>
						<td class="list-minus">-{{item.amount | number_format(0, '.', ',')}}</td>
					{% else %}
						<td class="list-plus">+{{item.amount | number_format(0, '.', ',')}}</td>
						<td class="list-minus"></td>
					{% endif %}
					<td><a href="edit.php?id={{item.event_id}}"><button class="btn edit">編集</button></a></td>
					<td>
						<form action="" method="post">
							<input type="hidden" name="event_id" value="{{item.event_id}}">
							<button class="btn delete" name="submit" value="delete">削除</button>
						</form>
					</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>
</div>
	