<!DOCTYPE html>
<html lang="ja">
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width, initial-scale=1.0">
	<script src="https://kit.fontawesome.com/5f5276d353.js" crossorigin="anonymous"></script>
	<script src="https://www.google.com/recaptcha/api.js"></script>
	<script src="https://accounts.google.com/gsi/client" async defer></script>
	<link rel="stylesheet" href="{{constant('lib\\common\\Bootstrap::APP_URL')}}css/style.css">
	<title>{{title}} | 家計簿アプリ</title>
</head>
<body>
	<header class="{% if page is defined and page == 'admin' %} admin-bg-color{% endif %}">
		<div class="header-container">
			<div class="header-left">
				<a class="logo" href="{{constant('lib\\common\\Bootstrap::APP_URL')}}src/{% if page == 'admin' %}admin/{% endif %}top.php">
					<h1>家計簿アプリ</h1>
				</a>
			</div>

			{% if page is defined and page == 'admin' %}
				{% include 'component/admin_header_right.html.twig' %}
			{% else %}
				{% include 'component/header_right.html.twig' %}
			{% endif %}
		</div>
	</header>

	<main>
